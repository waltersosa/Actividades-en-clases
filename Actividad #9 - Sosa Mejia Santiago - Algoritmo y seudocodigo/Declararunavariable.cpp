#include <iostream>

using namespace std;

int main()
{
    float N1 = 3.141592;
    double N2 = 145.679200;

    cout<<"Variable float x 2 "<<(N1*2);
    cout<<"\n cudrado de la variable double"<<(N2*N2);

    // Declaracion de las variables char
    
    char a = 65;
    cout<<"\n el valor de la variable char a es "<< a;
    char b = 6;
    cout<<"\n el valor de la variable char b es "<< b;
    
    // Variable tipo cadena de caracteres
    char flat =;
    cout<<endl<<"El valor de la variable float "<<flat;
    cout<<endl<<"El valor de la variable float negada "<< !flat;

    // Variables tipo cadena de caracteres
    string ciudad = "Esmeraldas"
    string nombre;
    cout<<endl<<"Ingresa tu nombre"

    return 0;

}